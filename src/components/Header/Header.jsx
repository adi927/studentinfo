import styles from "./Header.module.css";

const Header = () => {
  return (
    <div className={styles.header}>
      <div className={styles.wrapper}>
        <p>INFORMACJE O UCZNIU</p>
      </div>
    </div>
  );
};

export default Header;
